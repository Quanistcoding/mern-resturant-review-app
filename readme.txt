Go into noth server and client folders and run npm i to install all dependncies.
Then create a .env file in the server directory and defeine RESTURANTREVIEW_MONGO_URI for 
conneting to mongodb.

Run node index in server and npm start in client to start this app.